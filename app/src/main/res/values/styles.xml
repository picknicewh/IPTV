<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->

    </style>
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">#FF0000</item>
        <item name="colorPrimaryDark">#FF0000</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- 设置无标题-->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置全屏-->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="textStyle">
        <item name="android:layout_marginTop">@dimen/space_x1.5</item>
        <item name="android:textSize">@dimen/ts_x_large</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingLeft">@dimen/space</item>
        <item name="android:paddingRight">@dimen/space_x2</item>
        <item name="android:paddingTop">@dimen/space_x0.5</item>
        <item name="android:paddingBottom">@dimen/space_x0.5</item>
        <item name="android:focusable">true</item>
    </style>
    <!-- IPTV 文字风格 -->
    <style name="MyTextViewStyle">
        <item name="android:textSize">@dimen/ts_large</item>
        <item name="android:gravity">center</item>
        <item name="android:focusable">true</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
